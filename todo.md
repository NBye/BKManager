# ES Bookmark Manager 源码优化清单

> 审查范围：`src/` 全部 TypeScript、Manifest、构建配置、备份导入导出和离线同步流程。
>
> 优先级：`P0` 数据安全或核心正确性，`P1` 稳定性和明显性能问题，`P2` 架构与维护性，`P3` 体验和增强项。
>
> 当前基线：`npx tsc --noEmit`、`npm run build`、`git diff --check` 已通过；项目暂无自动化测试和 lint 脚本。

## P0：必须优先处理

### [x] P0-1 保留删除标记，避免离线设备复活数据

- **问题**：删除操作最终通过 `bulkDelete` 从 ES 物理删除；如果另一台浏览器还保留旧节点，下一次同步会把这个旧节点重新上传，导致已删除收藏复活。
- **证据**：`src/sync.ts:93` 先对合并结果执行 `sanitizeNodes`，删除节点被过滤；`src/sync.ts:106-107` 随后直接批量写入和删除；`src/app.ts:136-139` 只在本地创建删除操作。
- **建议**：把删除从“立即物理删除”改成“可同步的软删除墓碑”。
- **具体方案**：
  1. 同步合并时保留带 `deletedAt` 的原始节点，不要在上传前调用会过滤删除节点的清洗函数。
  2. 删除节点以带 `deletedAt` 的文档写入 ES，使其他设备能够收到删除事实。
  3. 展示层继续过滤墓碑；仅在明确的清理任务中删除超过保留期的墓碑，例如保留 30 天。
  4. 保留旧版本已物理删除数据的兼容说明；无法恢复的历史删除不应在升级时被错误复活。
- **验收标准**：浏览器 A 删除并同步；浏览器 B 在离线状态保留旧缓存，重新联网同步后节点消失，且不会再次出现在 ES 或任一设备。

### [x] P0-2 禁止初始化流程自动删除远端异常数据

- **问题**：首次初始化或本地 Profile 为空时，代码把清洗后未通过校验的远端节点直接判定为无效并调用 `bulkDelete`，可能误删孤儿节点、循环目录、未知未来类型或字段损坏的数据。
- **证据**：`src/sync.ts:146-154` 在读取远端后调用 `sanitizeNodes`，再删除所有不在清洗结果中的远端 ID。
- **建议**：读取、修复、删除三种动作必须分离；读取失败或数据异常不应自动产生破坏性操作。
- **具体方案**：
  1. 初始化只保存原始远端数据和可显示的清洗结果，不执行 `bulkDelete`。
  2. 将异常节点放入本地“待修复/隔离”集合，记录原因和原始内容。
  3. 在管理页提供“检查数据”和“确认清理”操作，用户确认后才删除异常节点。
  4. 对未知 `nodeType` 先兼容保留，等 schema 版本确认后再决定迁移或删除。
- **验收标准**：远端存在异常节点时，打开插件不会删除它；用户执行明确清理前，ES 文档数量和原始字段保持不变。

### [x] P0-3 加强 Profile 隔离并保护 ES 连接安全

- **问题**：本地 Profile Key 只由 ES 地址和索引名组成；同一地址/索引切换不同 API Key 时会共用本地缓存和待同步队列，存在跨账号显示或误上传风险。同时配置允许 `http://`，API Key 可能明文通过网络发送。
- **证据**：`src/types.ts:66-68` 的 `getProfileKey` 不包含凭据或租户标识；`src/es.ts:8-16` 将 API Key 放入请求头；`src/config.ts:6-11` 未限制 ES 地址必须使用 HTTPS。
- **建议**：建立显式工作区身份，并默认只允许安全连接。
- **具体方案**：
  1. 增加 `workspaceId`/租户标识；Profile Key 使用地址、索引和工作区标识组合，不直接保存 API Key。
  2. 没有工作区标识时，对 API Key 做本地 SHA-256 指纹作为隔离因子，并设计 Key 轮换迁移提示。
  3. 默认要求 ES 使用 `https://`；仅对 `localhost`/开发地址提供明确的开发例外。
  4. 配置保存、JSON 导入和请求前统一校验，不在错误信息、日志或备份中输出完整 API Key。
- **验收标准**：相同 ES 地址和索引下使用不同账号时，本地缓存互不读取；生产配置使用 HTTP 会被拒绝并给出清晰提示。

## P1：稳定性与性能

### [x] P1-1 把每次全量同步改为增量同步

- **问题**：每次打开浮窗、点击刷新或后台重试都会读取 ES 全部节点，并把合并后的全部节点重新 bulk upsert；数据量增大后网络、ES 写入和浏览器等待时间都会线性增长。
- **证据**：`src/popup.ts:495-506` 打开浮窗调用同步；`src/es.ts:89-111` 使用全量 `match_all`；`src/sync.ts:92` 全量拉取，`src/sync.ts:106` 全量写入。
- **建议**：默认增量同步，保留低频的全量校准。
- **具体方案**：
  1. 使用 `SyncMeta.lastSyncAt` 保存同步游标，查询 `updatedAt > lastSyncAt` 的远端变更。
  2. 上传只发送待同步操作涉及的节点和必要的兄弟排序变更。
  3. 删除墓碑使用独立变更记录或保留在同一索引中，不能依赖全量扫描发现删除。
  4. 每天或用户手动触发一次全量校准，并显示校准结果。
- **验收标准**：只有 1 个节点变化时，普通同步不会重写整个索引；全量校准仍能发现遗漏并修复本地缓存。

### [x] P1-2 为所有 ES 请求增加超时、取消和明确状态

- **问题**：`fetch` 没有超时控制，ES 不响应时浮窗可能长期停留在“正在同步”，用户无法区分网络断开、授权失败和服务端错误。
- **证据**：`src/es.ts:15-32` 的 `request` 直接等待 `fetch`。
- **建议**：统一请求生命周期管理。
- **具体方案**：
  1. 每个请求使用 `AbortController`，默认 10-15 秒超时。
  2. 允许手动刷新取消上一次请求，避免重复操作堆积。
  3. 将网络错误、超时、401/403、索引不存在、部分 bulk 失败转换成可识别错误类型。
  4. UI 显示“正在同步/已使用本地缓存/授权失败”等具体状态。
- **验收标准**：断开网络后打开浮窗，指定时间内回退到本地数据；不会无限等待，也不会阻塞后续离线编辑。

### [x] P1-3 使用可持久化的重试调度，完善退避策略

- **问题**：后台同步主要依赖内存 `setTimeout`；Manifest V3 Service Worker 被挂起后，定时器不可靠。`attempts` 字段存在但从未递增，也没有指数退避或最大重试状态。
- **证据**：`src/sync.ts:53-71` 使用内存定时器；`src/sync.ts:39-46` 读取旧 `attempts` 但不更新；`src/background.ts:9-10` 仅设置固定 1 分钟闹钟。
- **建议**：让队列重试依赖 Chrome Alarm 和持久化元数据。
- **具体方案**：
  1. 每次失败递增 `attempts`，记录 `lastAttemptAt`、`nextRetryAt` 和错误分类。
  2. 使用一次性 Alarm 调度下一次重试，延迟按 1 分钟、2 分钟、5 分钟、15 分钟递增并设置上限。
  3. Service Worker 启动时扫描未完成队列，按 `nextRetryAt` 恢复调度。
  4. 连续失败达到上限后保留队列并显示“需要手动重试”，不能静默丢弃。
- **验收标准**：Service Worker 重启后未同步操作仍能继续重试；失败操作始终留在队列中并可查看原因。

### [x] P1-4 将本地节点、操作和元数据写入合并为事务

- **问题**：一次创建/修改会先写节点、再写操作、再写 Meta，批量排序还会逐节点执行；中途浏览器崩溃可能留下节点与队列不一致的状态。
- **证据**：`src/app.ts:43-46` 循环调用保存；`src/sync.ts:34-50` 分三次访问 IndexedDB；`src/db.ts:45-58` 每次写入都单独打开数据库和事务。
- **建议**：以“一个用户动作”为单位提交原子本地变更。
- **具体方案**：
  1. 在 `db.ts` 增加跨 `nodes`、`operations`、`meta` Object Store 的统一 readwrite 事务。
  2. `persistNodes` 一次性写入全部节点、合并同一 nodeId 的队列操作并更新 Meta。
  3. 同一节点只保留最终动作：创建后修改合并为 create，创建后删除合并为 delete。
  4. 增加事务失败后的重读校验和恢复提示。
- **验收标准**：模拟写入中断后，不会出现“节点已保存但没有同步队列”或“队列存在但节点不存在”。

### [x] P1-5 处理 ES Bulk 的逐项结果，不把部分成功当成整体失败

- **问题**：当前只检查 `errors` 并取第一条错误；部分文档可能已经成功，失败文档和成功文档都会被整体保留重试，缺少逐项确认和用户可定位信息。
- **证据**：`src/es.ts:78-87` 的 `assertBulkSucceeded` 没有返回成功/失败 ID 映射。
- **建议**：让 Bulk 操作具备逐项确认能力。
- **具体方案**：
  1. 解析每个 item 的 `_id`、状态码和错误原因。
  2. 只移除已确认成功的操作，失败项保留在队列并记录原因。
  3. 对 429、5xx、权限错误分别采用退避、重试和人工修复策略。
  4. UI 提供失败节点数量和重试入口。
- **验收标准**：Bulk 部分失败时，成功项不会无限重复写入，失败项不会被误标记为已同步。

### [x] P1-6 增加远端、备份和导入数据的运行时 schema 校验

- **问题**：远端响应和备份文件通过类型断言直接转成 `BookmarkNode[]`；当前仅检查部分 URL 和 text 内容，缺少 ID、时间、排序、父级和字段组合校验。
- **证据**：`src/es.ts:72-75` 直接使用 `_source`；`src/manager.ts:197-208` 将 JSON 节点直接断言；`src/types.ts:3-17` 大多数字段为可选。
- **建议**：把 TypeScript 类型和运行时数据校验分开建设。
- **具体方案**：
  1. 定义 `folder`、`bookmark`、`text` 三个判别联合类型，去掉不适用字段的可选泛化。
  2. 增加 `parseNode`/`parseBackup`，严格校验 ID、nodeType、parentId、sortOrder、updatedAt 和字段组合。
  3. 校验父级必须是文件夹、目录无环、文本必须有非空 content、链接必须是 HTTP/HTTPS。
  4. 无效数据进入隔离报告，不直接参与同步或删除。
- **验收标准**：损坏的 ES 文档或备份不会导致页面崩溃、错误删除或静默丢失，并能指出具体节点和字段。

### [x] P1-7 修复“全量覆盖备份”只覆盖本地缓存的问题

- **问题**：导入覆盖模式只读取当前本地 Profile；如果 ES 上存在本地未缓存的节点，随后同步时可能被重新合并回来，因此“覆盖 ES 当前数据”的语义不成立。
- **证据**：`src/manager.ts:210-216` 只对 `getNodes(profileKey)` 结果做删除和导入，未先读取远端全量状态。
- **建议**：在覆盖前建立明确的远端快照和可恢复点。
- **具体方案**：
  1. 覆盖操作前先执行一次同步并读取 ES 全量节点。
  2. 生成本地备份快照，展示将删除/新增/修改的数量。
  3. 使用墓碑或版本化批次表达“远端不在备份中的节点全部删除”。
  4. 覆盖成功后写入一条恢复记录，失败时保留队列并允许撤销。
- **验收标准**：确认覆盖后，ES、本地缓存和管理页只包含备份内容；未缓存的远端节点不会复活。

### [x] P1-8 处理时间戳回拨和相同时间的确定性合并

- **问题**：冲突选择完全依赖客户端 `Date.now()`；不同浏览器系统时间不一致或同毫秒更新时，可能选择旧数据，且相同时间没有稳定的第二排序依据。
- **证据**：`src/sync.ts:11-13` 只比较 `updatedAt`；`src/types.ts:53-55` 使用客户端时间。
- **建议**：不必实现复杂并发编辑，但必须保证跨设备时间异常时结果可预测。
- **具体方案**：
  1. 增加单调的 `revision`/服务端更新时间；客户端时间只作为展示字段。
  2. 过渡期使用 `updatedAt`、`updatedBy`、`revision` 的固定比较顺序。
  3. 检测本机时间明显回拨，提示并避免生成比本地旧的更新版本。
  4. 对移动、删除、编辑统一使用同一版本生成函数。
- **验收标准**：相同时间戳和时钟回拨场景下，每台设备得到一致结果，不会因偶然 Map 遍历顺序产生不同结果。

## P2：架构与维护性

### [x] P2-1 建立真正的节点领域模型和统一业务服务

- **问题**：`popup.ts`、`manager.ts`、`tree.ts` 各自判断 `bookmark/folder/text`；配置同步、编辑、复制、统计等逻辑分散，新增节点类型容易漏改。
- **建议**：将节点行为集中到领域层，界面只负责渲染和派发动作。
- **具体方案**：
  1. 用判别联合类型定义三种节点的必需字段。
  2. 提供 `getNodeTitle`、`getNodeIcon`、`openNode`、`copyNode`、`countNodes` 等统一函数。
  3. 将 popup 和 manager 共用的编辑、删除、拖拽更新、状态提示抽到 controller/service。
  4. 通过节点类型映射表替代散落的三元表达式和重复 if 分支。
- **验收标准**：新增一个节点类型时，只需修改类型定义、领域服务和渲染映射，不需要在多个页面手工搜索补分支。

### [x] P2-2 增加 IndexedDB schema 版本和迁移机制

- **问题**：数据库版本固定为 `1`，目前新增 text 依赖字段兼容，但未来字段、索引或删除墓碑策略变化没有迁移入口。
- **证据**：`src/db.ts:3-24` 固定 `DB_VERSION = 1`，升级回调只创建 Object Store。
- **建议**：把本地数据库当作有版本的持久化协议维护。
- **具体方案**：
  1. 每次字段或索引变化递增 DB 版本。
  2. 在 `onupgradeneeded` 中为旧节点补默认值、迁移旧 Profile Key、创建新索引。
  3. 对不可迁移数据保留原始备份并显示迁移报告。
  4. 增加旧版本数据库升级测试。
- **验收标准**：从当前版本升级到下一版本后，旧收藏、text 节点、队列和 Meta 均可读取且不丢失。

### [ ] P2-3 建立自动化测试、lint 和 CI 基线

- **问题**：当前没有测试或 lint，只能依赖类型检查、构建和手工点击；同步删除、循环目录、离线迁移等高风险逻辑缺少回归保护。
- **建议**：优先测试纯函数和同步状态转换，再补浏览器端集成测试。
- **具体方案**：
  1. 引入轻量测试工具，覆盖 `sanitizeNodes`、标题生成、URL 去重、目录循环校验、排序和墓碑合并。
  2. 使用 fake IndexedDB 测试队列持久化和事务回滚。
  3. 使用 fetch mock 覆盖 ES 超时、分页、Bulk 部分失败、权限错误和离线重试。
  4. 增加 `test`、`lint`、`check` 脚本，在 CI 中执行类型检查、测试和生产构建。
- **验收标准**：每个 P0 同步场景都有自动化用例；提交前能一条命令发现类型、测试和构建回归。

### [x] P2-4 合并重复的配置和表单逻辑

- **问题**：`popup.ts`、`manager.ts`、`options.ts` 分别实现配置读取、JSON 双向同步、保存和连接测试；后续修复容易只改一个入口。
- **证据**：`src/popup.ts:69-94`、`src/manager.ts:41-91`、`src/options.ts:18-46` 都包含相似逻辑。
- **建议**：保留一个配置 service 和一个可复用配置编辑器。
- **具体方案**：
  1. 抽出 `parseConfig`、`serializeConfig`、`validateConfig`、`saveAndSyncConfig`。
  2. 抽出表单/JSON 双向绑定控制器，统一防止输入事件回写循环。
  3. 废弃只做跳转的旧 `options.ts` 业务入口，确保设置只有一个真实实现。
  4. 为三种入口统一连接失败和离线提示。
- **验收标准**：表单模式、JSON 模式、设置页、浮窗配置使用同一套校验和保存结果。

## P3：体验和增强项

### [x] P3-1 自动同步时先展示本地缓存，再后台更新

- **问题**：打开浮窗时先等待 `syncNow`，网络慢时用户无法立即看到已有本地收藏。
- **证据**：`src/popup.ts:495-506` 在 `refresh()` 前等待在线同步。
- **建议**：采用“本地立即可用，远端随后刷新”的交互。
- **具体方案**：
  1. 打开时先读取并渲染 IndexedDB，保留当前路径和布局。
  2. 后台执行同步，成功后局部刷新或保留滚动位置。
  3. 标题栏显示同步状态，失败只显示轻提示，不覆盖路径栏。
  4. 手动刷新继续提供强制等待和旋转状态。
- **验收标准**：网络延迟或断网时，浮窗在本地读取完成后立即可操作。

### [x] P3-2 统一复制提示和文本节点交互

- **问题**：浮窗已使用独立 toast，但管理页 text 点击复制仍复用底部状态栏；文本图标、键盘激活和复制失败提示在两个页面不一致。
- **建议**：把复制作为统一节点动作。
- **具体方案**：
  1. popup 和 manager 共用 toast 组件。
  2. 文本节点的图标统一处理换行、空格和 Unicode 字符，标题先规范化空白再截取。
  3. 宫格文本卡片支持 Enter/Space 复制，链接和文件夹保留各自快捷键。
  4. 剪贴板失败时显示可重试提示，不静默吞错。
- **验收标准**：树状、宫格、管理页点击或键盘操作 text 都复制完整原文，并在不改变路径导航的情况下提示结果。

### [x] P3-3 改善右键收藏的反馈和菜单生命周期

- **问题**：后台右键“加入BKM收藏”失败时直接 `catch(() => undefined)`，用户无法知道是否保存成功；Service Worker 每次启动都先清空再重建菜单，存在短暂菜单不可见窗口。
- **证据**：`src/background.ts:13-45` 每次加载调用 `removeAll`；`src/background.ts:78-80` 静默忽略 text 创建失败。
- **建议**：让右键操作可确认、可追踪。
- **具体方案**：
  1. 右键创建成功后更新扩展 badge 或发送短时通知；失败显示错误通知。
  2. 创建菜单使用固定 ID 和幂等注册，只有安装/升级或菜单缺失时重建。
  3. 将“未配置 ES”明确显示为“已保存到离线数据”，避免用户误以为丢失。
  4. 为受限页面、空选区和超长选区给出明确行为说明。
- **验收标准**：右键保存成功/失败都有用户可见结果；Service Worker 重启不影响菜单长期存在。

### [x] P3-4 收紧权限与完善发布流程

- **问题**：Manifest 允许所有 HTTP/HTTPS 主机，且仓库虽然通过 `.gitignore` 忽略 `*.crx`/`*.pem`，仍需明确发布文件不进入源码交付；开发流程没有自动检查产物是否与 Manifest 一致。
- **建议**：区分开发权限、生产权限和发布产物。
- **具体方案**：
  1. 评估改用 `optional_host_permissions`，在用户保存 ES 地址后申请对应域名权限。
  2. 生产默认关闭 HTTP，仅保留 HTTPS；开发配置单独处理。
  3. 增加发布检查：Manifest 引用的每个入口和图标必须存在，dist 不包含敏感配置。
  4. 在 README 中记录重新构建、重新加载扩展和权限变更流程。
- **验收标准**：发布检查能阻止缺失入口、错误 Manifest 路径和敏感文件进入发布包。

## 推荐实施顺序

1. 先完成 `P0-1` 删除墓碑和 `P0-2` 禁止初始化自动删除，先消除数据复活/误删风险。
2. 再完成 `P0-3` Profile 隔离及 HTTPS 校验，避免不同账号之间污染本地数据。
3. 接着完成 `P1-2`、`P1-3`、`P1-4`，保证网络异常、Service Worker 重启和本地崩溃时队列不丢。
4. 在同步安全稳定后做 `P1-1` 增量同步和 `P1-6` 运行时 schema 校验，降低性能成本并提升脏数据处理能力。
5. 最后推进 `P2` 架构/测试和 `P3` 体验优化。
